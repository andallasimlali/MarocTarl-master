@model IEnumerable<MarocTarl.Models.Exam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Niveau)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Matiere)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateEffet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PalierOrdre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Obligatoire)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Niveau)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Matiere)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DateEffet)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.PalierOrdre)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Title)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Obligatoire)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Paliers","index", "Paliers", new { id = item.Id },null) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
    </td>
</tr>
}

</table>

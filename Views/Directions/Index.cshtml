@model IEnumerable<MarocTarl.Models.Direction>

@{
    ViewBag.Title = "Index";
    var id = ViewBag.id;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create" , new { id = id})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Academie.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Academie.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Ecoles", "index", "etabs", new { id=item.Id }, null) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

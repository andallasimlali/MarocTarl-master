@model IEnumerable<MarocTarl.Models.Exercice>

@{
    ViewBag.Title = "Index";
    var id = ViewBag.id;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { id = id })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Palier.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ordre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShowWithPrevious)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Seuil)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Palier.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ordre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShowWithPrevious)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seuil)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

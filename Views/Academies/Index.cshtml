@model IEnumerable<MarocTarl.Models.Academie>

@{
    ViewBag.Title = "Index";
    var MinistereId = ViewBag.MinistereId;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create" , new { id = MinistereId })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ministere.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ministere.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Directions", "index", "Directions", new { id=item.Id },null) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

@model IEnumerable<MarocTarl.Models.Palier>

@{
    ViewBag.Title = "Index";
    var id = ViewBag.id;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { id =id})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Exam.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ordre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsCompetence)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Exam.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ordre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsCompetence)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Exercices", "index", "Exercices", new { id=item.Id },null) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
